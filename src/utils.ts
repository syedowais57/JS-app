// Build pass test
export function greet(name: string) {
    return `Hello, ${nme}! 👋`;
}
function greet2(nme: string) {
    return `not, ${name}! 👋`;
}
function greet3(name: string) {
    return `not, ${name}! 👋`;
}